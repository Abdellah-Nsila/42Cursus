#ifndef PUSH_SWAP_H
# define PUSH_SWAP_H

# include <unistd.h>
# include <stdio.h>
# include <fcntl.h>
# include <stdlib.h>
# include <string.h>

// handle double linked list

typedef struct s_node 
{
    int n;
    struct s_node   *prev;
    struct s_node   *next;
} t_node ;


// commands for push - swap - rotate
// handle inputs

#endif 